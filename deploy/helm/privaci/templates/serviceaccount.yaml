{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "privaci.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "privaci.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
